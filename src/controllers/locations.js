export const getLocations = (req, res) => {
    try {
        res.json('sucess');
    } catch(error) {
        res.status(405).json(error);
    }
};

export const createLocation = (req, res) => {
    try {
        res.json('sucess');
    } catch(error) {
        res.status(405).json(error);
    }
};

export const returnLocation = (req, res) => {
    try {
        res.json('sucess');
    } catch(error) {
        res.status(405).json(error);
    }
};